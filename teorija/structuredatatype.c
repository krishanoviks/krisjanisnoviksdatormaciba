#include <stdio.h>
#define N 3


struct my_struct {
  char name[20];
  int countI;
  double countF;
}

int main(){

  struct mystruct work[N];
  printf("ievadiet %d. vardu ",i+1);
  scanf("%s",work.name[i]);
  printf("ievadiet %d. int vertibu ",i+1);
  scanf("%d",work.countI[i]);
  printf("ievadiet %d. double vertibu ",i+1);
  scanf("%f",work.countF[i]);


}
